**NavItem** — a row in the barn sidebar; `active` paints the neon wash + left gradient bar, `count` adds a trailing badge.

```jsx
<NavItem icon="flame" label="Discover" active />
<NavItem icon="heart" label="Matches" count={7} />
<NavItem icon="settings" label="Barn Settings" />
```
