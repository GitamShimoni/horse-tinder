**Tag** — soft glass chip for profile traits; pass an `emoji` prop for the leading glyph.

```jsx
<Tag emoji="🌾">Organic Hay Only</Tag>
<Tag emoji="🏎️">1 HP (Literally)</Tag>
```

Wrap several in a flex row with `gap`.
