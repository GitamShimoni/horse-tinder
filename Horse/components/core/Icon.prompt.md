**Icon** — a stroke icon from Trotr's curated Lucide set; inherits `currentColor` and scales with `size`.

```jsx
<Icon name="heart" size={22} />
<Icon name="crown" size={16} style={{ color: 'var(--accent-premium)' }} />
```

Names: heart, x, zap, star, sparkles, crown, flame, mapPin, check, settings, shoppingBag, store, messageCircle, bell, activity, wine, gem, shieldCheck, chevronRight, plus, bluetooth, apple, carrot, home, users, rotateCcw. Color it by setting `color` on the element or a parent.
