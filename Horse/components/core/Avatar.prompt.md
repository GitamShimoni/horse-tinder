**Avatar** — circular identity token; falls back from `src` → `emoji` → `initials`. Add a gradient `ring` for premium members.

```jsx
<Avatar emoji="🐴" size="xl" ring="neon" online />
<Avatar initials="ST" size="sm" />
```

Sizes: xs · sm · md · lg · xl. Rings: none · neon · gold · emerald.
