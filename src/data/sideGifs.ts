/**
 * Decorative GIFs shown in the rails on either side of the swipe deck
 * (desktop ≥1100px only). Empty by default — the rails don't render until
 * you add GIFs here.
 *
 * To add yours: drop the files into `public/gifs/` and list their public
 * paths below, e.g.
 *   export const leftGifs = ["/gifs/gallop.gif", "/gifs/hay.gif"];
 *   export const rightGifs = ["/gifs/wink.gif"];
 */
export const leftGifs: string[] = [];
export const rightGifs: string[] = [];
