**GlassCard** — the frosted-glass surface everything sits on; add a `glow` to make marketplace/dating cards bloom, `interactive` for clickable ones.

```jsx
<GlassCard glow="pink" padding="var(--space-6)">…</GlassCard>
<GlassCard interactive glow="gold">Product</GlassCard>
```

Props: `glow` (none/pink/orange/gold/emerald), `strong` (heavier blur), `interactive`, `padding`, `radius`.
