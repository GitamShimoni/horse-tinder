**ProductCard** — a tile in the Neigh-Premium merch shop; tone-tinted hero, mono price, buy button. `gold` for premium, `emerald` for new, `neon` for hot.

```jsx
<ProductCard
  emoji="🐴"
  name="Smart Horseshoe Pro"
  blurb="Bluetooth step tracking + heart-rate monitor for when they spot an attractive mare."
  price="$399"
  tone="gold"
/>
```
